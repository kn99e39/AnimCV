/result
__pycache__/
*.pyc
.venv/
.venv_build/
.venv_build_full/
.venv_build_gui/
*.egg-info/
/build_output
*.blend1

# Large, fully regenerable training intermediates (see scripts/training/)
/training_cache/

# Owner-facing visual QA media.  Retained locally under the project root, but
# intentionally excluded from Git because it is 263 MB of regenerable review artifacts.
/user_QE/

# Reassembled from datasets/mpi_inf_3dhp/**/annot.mat.part-* by
# scripts/training/reassemble_mpi_inf_3dhp.sh; only the split parts are committed.
datasets/mpi_inf_3dhp/**/annot.mat
