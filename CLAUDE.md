# Working on this repo

This repo's agent/contributor instructions live in `AGENTS.md`, not here —
read that file. It covers multi-machine sync, LabServer63 experiment
execution, architecture precedence, branch layout, worklog conventions and
OS-specific scripts. This file exists only as a pointer, because Claude Code
looks for `CLAUDE.md` by name specifically; every other tool working on this
repo (including Codex) should read `AGENTS.md` directly. Keep this file to
this one pointer — do not fork content back into it, or the two will drift.
